<resources>    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary </item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/Background</item>
        <item name="layoutColor">@color/Layout</item>
        <item name="textColor">@color/Text</item>
        <item name="buttonColor">@color/Button</item>
        <item name="tintColor">@color/Tint</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>
    <style name="DarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_Dark </item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_Dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/Background_Dark</item>
        <item name="layoutColor">@color/Layout_Dark</item>
        <item name="textColor">@color/Text_Dark</item>
        <item name="buttonColor">@color/Button_Dark</item>
        <item name="tintColor">@color/Tint_Dark</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>
    <style name="MainActionBar.Popup" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/Background</item>
        <item name="android:textColor">@color/Text</item>
    </style>
    <style name="MainActionBar.Popup." parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:colorBackground">@color/Background_Dark</item>
        <item name="android:textColor">@color/Text_Dark</item>
    </style>
</resources>
